# Recenze — soubor s daty není veřejně přístupný (PHP zapisuje přes api/reviews.php)
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Deny from all
</IfModule>
